<div class="form-group row">
    <div class="col-lg-9 ml-auto">
        <a href="{{ url()->previous() }}" class="btn btn-default"><em class="fa fa-reply"></em> {{ __('global.cancel') }}</a>
        <button type="submit" class="btn btn-info"><em class="fa fa-save"></em> {{ __('global.submit') }}</button>
    </div>
</div>